{% extends 'admin/adminBase.html.twig' %}

{% block stylesheets %}

{% endblock %}

{% block body %}



    <div class="container">
        <div class="col-md-12">
            <h3>Liste des livraisons effectuées</h3>

            <div class="table-responsive">
                <table class="table">
                    <thead>
                    <tr>
                        <th scope="col">Concession</th>
                        <th scope="col">Date de livraison</th>
                        <th scope="col">Client</th>
                        <th scope="col">Mode de livraison</th>
                        <th scope="col">Marque</th>
                        <th scope="col">Modèle</th>
                        <th scope="col">Version</th>
                        <th scope="col">Actions</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for livraison in livraisons %}
                            <tr>
                                <td>{{ livraison.cartContent.vehiculePhysique.concession.nom }}</td>
                                <td>{{ livraison.dateLivraisonEffective }}</td>
                                <td>{{ livraison.cartContent.user.nom }} {{ livraison.cartContent.user.prenom }}</td>
                                <td>{{ livraison.modeDeLivraison }}</td>
                                <td>{{ livraison.cartContent.vehiculePhysique.version.model.marque }}</td>
                                <td>{{ livraison.cartContent.vehiculePhysique.version.model }}</td>
                                <td>{{ livraison.cartContent.vehiculePhysique.version }}</td>
                                <td>
                                    <a class="btn btn-primary" href="{{ path('livraison_edit', { 'id': livraison.id }) }}">Modifier</a>
                                </td>
                            </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>



{% endblock %}






{% block javascripts %}

{% endblock %}