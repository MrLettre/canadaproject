

{% extends 'base.html.twig' %}

{% block body %}



   <h1>Votre Panier</h1>


{% for cart in cart %}

    <div class="table-responsive">
        <table class="table">
            <thead>
            <tr>
                <th scope="col">Marque</th>
                <th scope="col">Modèle</th>
                <th scope="col">Version</th>
                <th scope="col">Kilometrage</th>
                <th scope="col">Photo</th>
            </tr>
            </thead>
            
                    <tbody>
                    <tr>
                        <td>{{ cart.vehiculePhysique.version.model.marque }}</td>
                        <td>{{ cart.vehiculePhysique.version.model }}</td>
                        <td>{{ cart.vehiculePhysique.version }}</td>
                        <td>{{ cart.vehiculePhysique.kilometrage }} km</td>
                        <td><a href="#myModal" role="button" ><img src="/images/photosVehiculePhysique/{{ cart.vehiculePhysique.imageName }}" width="80" height="auto"></a></td>
                    </tr>
                    </tbody>
           
        </table>
    </div>







{% else %}

<h4>Votre panier est vide</h4>

{% endfor %}



{% for cart in cart %}

<h5>Prix HT: {{ cart.vehiculePhysique.prixHT }} $</h5>

<h5>Prix TTC: {{ cart.vehiculePhysique.prixHT * 1.19 }} $</h5>

    <button type="button" class="btn btn-success"><a href="{{ path('cart_validation') }}">Valider le panier</a></button>
    <button type="button" class="btn btn-danger"><a href="{{ path('clearCart') }}">Supprimer le panier</a></button>


{% endfor %}






   
{% endblock %}