{% extends 'admin/adminBase.html.twig' %}

{% block body %}
    <h1>Editer un article</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}
    <br>
    <br>
    <p>
        <a href="/admin/article/{{ article.id }}/delete">Supprimer cet article</a>
    </p>
    <p>
            <a href="{{ path('article_admin_index') }}">Revenir à la liste</a>
    </p>


{% endblock %}
