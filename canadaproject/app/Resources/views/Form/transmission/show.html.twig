{% extends 'base.html.twig' %}

{% block body %}
    <h1>Transmission</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ transmission.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ transmission.nom }}</td>
            </tr>
            <tr>
                <th>Actif</th>
                <td>{% if transmission.actif %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('transmission_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('transmission_edit', { 'id': transmission.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
