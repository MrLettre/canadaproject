{% extends 'base.html.twig' %}

{% block body %}
    <h1>Couleurdisponible</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ couleurDisponible.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ couleurDisponible.nom }}</td>
            </tr>
            <tr>
                <th>Imagename</th>
                <td>{{ couleurDisponible.imageName }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('couleurdisponible_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('couleurdisponible_edit', { 'id': couleurDisponible.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
